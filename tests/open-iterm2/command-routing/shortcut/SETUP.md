# Scenario

**Feature**: command-routing / shortcut

Cmd+; shortcut command follows stored preference (default cd-only).

```go
func Setup(t *testing.T, req *Request) error {
	_ = t
	_ = req
	return nil
}
```