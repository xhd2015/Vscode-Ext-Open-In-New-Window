# Scenario

**Feature**: command-routing / codex-command

Palette **Open iTerm2: Codex** always runs codex regardless of stored shortcut.

```go
func Setup(t *testing.T, req *Request) error {
	_ = t
	_ = req
	return nil
}
```
