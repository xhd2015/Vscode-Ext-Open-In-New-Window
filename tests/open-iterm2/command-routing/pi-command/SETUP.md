# Scenario

**Feature**: command-routing / pi-command

Palette **Open iTerm2: Pi** always runs pi regardless of stored shortcut.

```go
func Setup(t *testing.T, req *Request) error {
	_ = t
	_ = req
	return nil
}
```
