# Scenario

**Feature**: command-routing / grok-command

Palette **Open iTerm2: Grok** always runs grok regardless of stored shortcut.

```go
func Setup(t *testing.T, req *Request) error {
	_ = t
	_ = req
	return nil
}
```