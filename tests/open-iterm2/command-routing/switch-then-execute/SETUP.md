# Scenario

**Feature**: command-routing / switch-then-execute

Switch Shortcut updates preference, then palette and shortcut commands behave independently.

```go
func Setup(t *testing.T, req *Request) error {
	_ = t
	_ = req
	return nil
}
```