# Scenario

**Feature**: command-routing / claude-command

Palette **Open iTerm2: Claude Code** always runs claude regardless of stored shortcut.

```go
func Setup(t *testing.T, req *Request) error {
	_ = t
	_ = req
	return nil
}
```
