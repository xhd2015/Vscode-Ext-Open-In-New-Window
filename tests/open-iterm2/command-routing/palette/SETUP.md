# Scenario

**Feature**: command-routing / palette

Palette **Open iTerm2** always runs cd-only regardless of stored Cmd+; preference.

```go
func Setup(t *testing.T, req *Request) error {
	_ = t
	_ = req
	return nil
}
```