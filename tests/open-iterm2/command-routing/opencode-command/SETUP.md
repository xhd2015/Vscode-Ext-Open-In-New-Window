# Scenario

**Feature**: command-routing / opencode-command

Palette **Open iTerm2: OpenCode** always runs opencode regardless of stored shortcut.

```go
func Setup(t *testing.T, req *Request) error {
	_ = t
	_ = req
	return nil
}
```
